# Gemini (필수) — Google AI Studio 에서 발급
GEMINI_API_KEY=

# Supabase (필수) — Project Settings > API Keys
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Google OAuth (선택) — Google Cloud Console > Credentials > Web client.
# 서버에서 ID token 검증 시 audience 비교에 사용.
GOOGLE_OAUTH_CLIENT_ID=
