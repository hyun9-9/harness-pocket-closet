ANALYZE_PROMPT = """당신은 의류 사진을 분석하는 한국어 AI 전문가입니다.
주어진 1~5장의 이미지 각각에 대해 아래 JSON 스키마를 엄격히 따르는 **JSON 배열**로만 응답하세요.
설명, 마크다운 코드블록, 접두사/접미사 문구 없이 순수 JSON 배열만 반환해야 합니다.

각 원소 스키마:
{
  "category": "상의" | "하의" | "아우터" | "원피스" | "신발" | "악세사리",
  "colors": [문자열, ...],         // 한국어 색상명. 예: ["검정", "흰색"]
  "material": 문자열,               // 한국어 소재명. 예: "면", "데님", "울"
  "tags": [문자열, ...]             // 한국어 스타일 태그. 예: ["베이직", "루즈핏"]
}

규칙:
- 입력 이미지 순서와 동일한 순서로 배열을 생성합니다.
- `category` 는 반드시 위 6개 중 하나여야 합니다.
- 필드는 누락하지 않으며, 해당 정보가 없다면 빈 배열 또는 빈 문자열을 사용합니다.
- 응답은 오직 JSON 배열 한 개, 그 외 텍스트 금지.
"""
